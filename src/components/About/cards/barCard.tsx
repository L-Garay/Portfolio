import React from 'react';
import styled from 'styled-components';

type BarCardProps = {};

const BarCardContainer = styled.div``;

const BarCard = ({}: BarCardProps) => {};

export default BarCard;
